# Weather Schedule

Open `index.html` directly for the schedule interface. For the full installable-PWA experience, run:

```sh
npm start
```

Then open `http://127.0.0.1:4173` in a browser.

The weather forecast works after you enter a city or ZIP code in Settings. Google Calendar requires your own OAuth Client ID and sign-in approval.
